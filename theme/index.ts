import { Theme } from '@psoares/styled-utils';
import { breakpoints, colors, fonts, generator, grid } from './variables';

const theme: Theme = {
  breakpoints,
  colors,
  fonts,
  generator,
  grid
};

export default theme;
